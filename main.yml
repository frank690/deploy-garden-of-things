- name: Update and prepare
  hosts: hosts
  become: true
  remote_user: root
  roles:
    - user-setup
  tags:
    - user-setup

- name: Configure UFW
  hosts: hosts
  become: true
  remote_user: root
  roles:
    - ufw
  tags:
    - ufw

- name: Install pip
  hosts: hosts
  become: true
  remote_user: nerf
  roles:
    - pip
  tags:
    - pip

- name: Install git and clone repos
  hosts: hosts
  become: true
  remote_user: root
  roles:
    - git
  tags:
    - git
